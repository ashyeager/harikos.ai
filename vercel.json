{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @harikos/web build && rm -rf .next && cp -a apps/web/.next .next",
  "crons": [{ "path": "/api/cron/trial-emails", "schedule": "0 9 * * *" }]
}
